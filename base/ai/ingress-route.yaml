apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: blocky
spec:
  entryPoints:
    - websecure
  routes:
  - kind: Rule
    match: Host(`st.kouse.xyz`)
    priority: 0
    services:
    - kind: Service
      name: sillytavern
      namespace: ai
      port: web
  tls:
    certResolver: default
    domains:
      - main: "kouse.xyz"
        sans:
          - "*.kouse.xyz"
