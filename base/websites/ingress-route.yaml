apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: websites
spec:
  entryPoints:
    - web
    - websecure
  routes:
  - kind: Rule
    match: Host(`bigbackstore.kouse.xyz`) || Host(`chad.kouse.xyz`) || Host(`lyrics.kouse.xyz`)
    middlewares: []
    priority: 0
    services:
    - kind: Service
      name: websites
      namespace: "websites"
      port: websites
  tls:
    certResolver: default
    domains:
      - main: "kouse.xyz"
        sans:
          - "*.kouse.xyz"