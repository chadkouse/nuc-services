apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: unifi

commonLabels:
  app: unifi

resources:
- namespace.yaml
- mongodb
- unifi-network-application
- ingress-route.yaml

patchesStrategicMerge:
- volumes_patch.yaml
