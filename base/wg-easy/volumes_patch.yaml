apiVersion: apps/v1
kind: Deployment
metadata:
  name: wg-easy
spec:
  template:
    spec:
      volumes: 
      - hostPath: 
          path: /opt/wg-easy/config 
          type: DirectoryOrCreate 
        name: config